import 'package:flutter/material.dart';

import '../../dock/dock_page.dart';

class DockScreen extends StatelessWidget {
  const DockScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const DockPage();
  }
}